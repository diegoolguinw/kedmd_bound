name: kedmd_env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11  # Adjust the Python version as needed
  - numpy
  - matplotlib
  - pandas
  - plotly
  - scipy
  - scikit-learn
  - ipython
  - kaleido